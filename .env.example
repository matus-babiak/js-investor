# Frontend (skopíruj do .env). Webhook = dáta do tabuľky len pri kliknutí na "Navrhnúť ucelený systém".
VITE_WEBHOOK_URL=https://hook.eu2.make.com/tvoj-webhook-id

# Ecomail – len na Verceli (Settings → Environment Variables), nie v .env:
# ECOMAIL_API_KEY=váš-api-kľúč
# ECOMAIL_LIST_ID=id-zoznamu-kontaktov
